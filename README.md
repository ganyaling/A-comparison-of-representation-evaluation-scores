# A-comparison-of-representation-evaluation-scores
The goal of the thesis would be to explore how these different scores behave and apply to different image datasets and  pre-trained models. Ideally, managing to create a competitive representation scoring method would be a plus
